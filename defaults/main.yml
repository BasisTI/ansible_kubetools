---
# defaults file for kubetools
# @var kubetools_kubectl_version:
# By default kubetools_kubectl_version is undefined, it will be get the stable version on the https://storage.googleapis.com/kubernetes-release/release/stable.txt.
kubetools_helm_version: v3.3.0
# @var kubetools_helm_version: v3.3.0
# You can specify the helm version.
kubetools_helmfile_version: v0.125.7
# @var helmfile_version: v0.125.7
# You can specify the helmfile version.
